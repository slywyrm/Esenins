import { BlogMessageDirective } from './blog-message.directive';

describe('BlogMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new BlogMessageDirective();
    expect(directive).toBeTruthy();
  });
});
