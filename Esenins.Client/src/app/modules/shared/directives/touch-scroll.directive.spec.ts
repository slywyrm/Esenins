import { TouchScrollDirective } from './touch-scroll.directive';

describe('TouchScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchScrollDirective();
    expect(directive).toBeTruthy();
  });
});
